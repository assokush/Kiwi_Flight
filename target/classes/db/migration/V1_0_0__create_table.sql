create table flight_log (
id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
flight_from varchar(256),
flight_to varchar(256),
response text NOT NULL,
system_date_time timestamp NOT NULL DEFAULT now()
)


